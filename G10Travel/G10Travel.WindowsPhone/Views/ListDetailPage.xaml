<Page
    x:Class="G10Travel.Views.ListDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:G10Travel.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource background}">

    <Grid x:Name="ListDetailsView" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Name="TitlePanel" Grid.Row="0" Margin="0,18,0,18">
            <TextBlock Text="{Binding Name}" Style="{StaticResource TitleTextBlockStyle}" Margin="18,0" HorizontalAlignment="Center" FontFamily="Berlin Sans FB Demi" FontSize="26.667" FontWeight="Bold" Foreground="#FFFBF2B7"/>
        </StackPanel>

        <Border BorderBrush="White" BorderThickness="2" HorizontalAlignment="Left" Height="580" Margin="0,0,0.167,0" Grid.Row="1" VerticalAlignment="Top" Width="400"/>
        <ListView Name="lvMyLists"  HorizontalAlignment="Left" Height="550" Margin="0" Grid.Row="1" VerticalAlignment="Top" Width="390">
            <CollectionViewSource x:Name="CollectionViewSource"  IsSourceGrouped="True" />
            <ListView.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Type}">
                            </TextBlock>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListView.GroupStyle>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="20">
                        <CheckBox Click="CheckBox_Click" IsChecked="{Binding ItemChecked, Mode=TwoWay}">
                            <TextBlock Text="{Binding ItemName}" Style="{ThemeResource ListViewItemTextBlockStyle}"/>
                        </CheckBox>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>

            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapGrid Orientation="Horizontal" ItemWidth="400" />
                </ItemsPanelTemplate>
                </ListView.ItemsPanel>




        </ListView>

        <Button Name="btnAddListItem" Content="+" FontSize="24" VerticalAlignment="Stretch" Background="#FFFDFDFD" Foreground="Black" MinWidth="10" MinHeight="10" RenderTransformOrigin="0.561,0.544" HorizontalAlignment="Stretch" Click="btnAddListItem_Click" Margin="0,518.167,0,-10" Grid.Row="1"/>


    </Grid>
</Page>
